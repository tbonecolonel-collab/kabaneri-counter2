カバネリカウンター v16

主な修正:
- 「操作エラー」の原因になりやすい localStorage 容量超過を修正。
- カスタムボタン画像は localStorage ではなく IndexedDB に保存。
- v15/v14 の画像設定を可能な範囲で自動移行。
- 通常のカウンターデータ保存時に画像データを除外し、ゲーム数・ベル・CZ記録などが止まらないように変更。
- 古い Service Worker / Cache は起動時に解除。

index.html を VS Code の Live Server で開いてください。


v22変更点
- 小吉がアイテムくじ1〜5回目の中で複数回出た場合、それぞれの出現位置を個別に判定
- 海門Lvはキャラ示唆とステージ示唆を統合し、重複Lvを強調表示。輪廻くじ示唆がある周期は輪廻くじを最優先
- 今までの記録を専用の軽量保存領域に保存し、現在データと分離。全データリセット前に履歴を保存してからリセット

[v39]
- 以前の「今までの記録」を削除して新規開始
- 保存記録ごとに「自分の推測設定」(設定1〜6)を保存可能


v40: 設定推測ロジック更新。ボーナス当選時100pt超割合とチャンス目発光率の新基準を無名・生駒・カバネ別に総合判定へ反映。
